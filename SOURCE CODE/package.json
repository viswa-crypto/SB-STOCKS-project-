{
  "name": "sb-stocks",
  "version": "1.0.0",
  "description": "SB Stocks - full stack stock trading simulator",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "concurrently \"npm run start --prefix client\" \"node server/server/server.js\""
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
